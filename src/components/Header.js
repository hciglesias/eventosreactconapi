import React from 'react';

const Header = () => {
    return (
        <header className="uk-margin" uk-margin="true">
            <h1 className="uk-text-center">Eventos en React con EventBrie API</h1>
        </header>
    );
};

export default Header;